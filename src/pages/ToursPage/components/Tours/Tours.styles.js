import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  title: {
    marginBottom: "4rem",
  },
  underline: {
    width: "6rem",
    height: "0.25rem",
    background: "#49A6E9",
    margin: "0 auto",
  },
});
